apiVersion: apps/v1
kind: Deployment
metadata:
  name: events-api
  namespace: ab-calicritters
  labels:
    app: events-api
    track: stable
spec:
  replicas: 2
  selector:
    matchLabels:
      app: events-api
      track: stable
  template:
    metadata:
      labels:
        app: events-api
        track: stable
    spec:
      containers:
        - name: events-api
          image: ab-calicritters/events-api:stable
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: SERVICE_VARIANT
              value: "stable"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ab-calicritters-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 20
