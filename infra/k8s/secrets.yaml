apiVersion: v1
kind: Secret
metadata:
  name: ab-calicritters-secrets
  namespace: ab-calicritters
type: Opaque
stringData:
  # Do not commit real credentials.
  # scripts/minikube/deploy.ps1 replaces this in-cluster from local .env.
  DATABASE_URL: "__REPLACE_WITH_DATABASE_URL__"
