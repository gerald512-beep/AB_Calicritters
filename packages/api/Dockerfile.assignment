FROM node:20-alpine

WORKDIR /app
COPY . .

RUN npm ci --include=dev \
  && npm run prisma:generate \
  && npm run build -w @ab-calicritters/api

ENV NODE_ENV=production
ENV PORT=3000

EXPOSE 3000
CMD ["npm", "run", "start:assignment-api", "-w", "@ab-calicritters/api"]
