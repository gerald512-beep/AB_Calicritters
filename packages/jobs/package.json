{
  "name": "@ab-calicritters/jobs",
  "version": "1.0.0",
  "private": true,
  "description": "Analytics jobs worker for AB Calicritters",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "rollup": "tsx src/rollupCli.ts",
    "build": "tsc -p tsconfig.json",
    "check:idempotency": "tsx src/checkIdempotency.ts",
    "loadtest:run": "tsx src/loadtest/runLoadTest.ts",
    "loadtest:assert": "tsx src/loadtest/assertLoadTest.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "dotenv": "^16.4.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.8",
    "artillery": "^2.0.22",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
